{
  "name": "utils",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist ; tsc ; rollup -c rollup.config.js ; cd ./dist ; rni $(Get-ChildItem | Select-Object -First 1) index.js ; node . ; cd ..",
    "run":"2+5"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@open-wc/building-rollup": "^2.0.1",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.66.0",
    "typescript": "^4.5.5"
  }
}
